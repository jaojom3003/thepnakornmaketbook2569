# MarketBook - ระบบจองล็อคตลาดนัด

Web Application สำหรับจองล็อคตลาดนัด พร้อม AI ผู้ช่วยแนะนำทำเลขายของ (Powered by Google Gemini & Supabase)

## Features
- 🗺️ แผนที่ตลาดแบบ Interactive
- 🤖 **AI Market Guru**: แนะนำทำเลขายของผ่าน Supabase Edge Function (Secure Backend)
- 📝 ระบบจองล็อค
- 📱 Responsive Design

## การติดตั้งและตั้งค่า (Setup Guide)

### 1. ตั้งค่า Supabase Project
1. สร้างโปรเจกต์ที่ [Supabase Dashboard](https://supabase.com/dashboard)
2. สร้างไฟล์ `.env` ที่ root folder และใส่ค่าจาก Supabase (สำหรับการ Run ในเครื่อง):
   ```env
   VITE_SUPABASE_URL=https://your-project-ref.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key-here
   ```

### 2. ตั้งค่า Backend (Edge Function)
เปิด Terminal และรันคำสั่งตามลำดับ:

1. **Login เข้า Supabase CLI:**
   ```bash
   npx supabase login
   ```

2. **Deploy Function ไปที่ Supabase:**
   ```bash
   npx supabase functions deploy gemini-proxy --no-verify-jwt
   ```

3. **ตั้งค่า API Key (ความลับ) บน Server:**
   แทนที่จะใส่ Key ในโค้ด เราจะส่งไปเก็บที่ Server โดยตรง:
   ```bash
   npx supabase secrets set API_KEY=ใส่_GEMINI_API_KEY_ของจริงที่นี่
   ```

### 3. รันโปรแกรม (Local Development)
```bash
npm run dev
```

---

## 🚀 การนำขึ้นใช้งานจริง (Deployment)

แนะนำให้ใช้ **Vercel** ในการโฮสต์หน้าเว็บ (Frontend)

1. **นำโค้ดขึ้น GitHub**
2. **ไปที่ Vercel.com** แล้วกด **Add New Project** -> **Import** จาก GitHub
3. **ตั้งค่า Environment Variables** ใน Vercel (สำคัญมาก!):
   * `VITE_SUPABASE_URL`: ค่าเดียวกับใน .env
   * `VITE_SUPABASE_ANON_KEY`: ค่าเดียวกับใน .env
4. **กด Deploy**

**หมายเหตุ:** ไฟล์ `vercel.json` ในโปรเจกต์ถูกเตรียมไว้แล้วเพื่อให้รองรับการทำงานแบบ Single Page Application (SPA)

---

## 🛠️ การใช้งานระบบ Admin
* **URL:** เข้าใช้งานได้ที่ปุ่ม "เข้าสู่ระบบ Admin" มุมขวาบน
* **Username:** `adminmarketbook`
* **Password:** `147`
* **Database Setup:**
    1. เข้าสู่ระบบ Admin
    2. กดปุ่ม "แสดง SQL สร้างตาราง" -> Copy ไปรันใน Supabase SQL Editor
    3. กดปุ่ม "ลงข้อมูลเริ่มต้น (Seed Data)" เพื่อใส่ข้อมูลตัวอย่าง